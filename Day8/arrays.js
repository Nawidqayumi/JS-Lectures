// An array is a special type of variable that can hold more than one value.


// create an array :

// 1-:
 var scores_1 = new Array(); //the score array is empty, holds no element

//  2-:
var scores_2 = Array(10); //The scores array with initial size.

// 3-:

var scores_3 = new Array(9,10,8,7);

// 4-:

var colors = ["red", "green", "blue"]; //colors array with three elements

// 5-:
var emptyArray =[]; //creating an empty array


// Accessing array elements:
// you can access elements of an array by using the squre brackets[]. the first element of the array start with 0, the second element starts with 1, and so on. 


var colors = ["red", "green", "blue"]; 
//              0       1       2  => inex


            // 1- colors[0] => red
            // 2- colors[1] => green






// do while loop:
/*
- repeatedly execute same block of statement when the codition is true. 
- the statments execute once intially then the condition is checked at loop end. 
- code in loop body will execute at least once
*/

// do {
//     // statments
// } while (condition);

var num1 = 0;

do{
    console.log(`Our number is : ${num1}`);
    num1++;
} while (num1<4);

